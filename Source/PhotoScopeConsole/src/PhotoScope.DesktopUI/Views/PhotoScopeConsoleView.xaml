<UserControl x:Class="PhotoScope.DesktopUI.Views.PhotoScopeConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoScope.DesktopUI.Views"
             mc:Ignorable="d" Height="500" Width="1000" >
    <Grid Margin="25">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:SearchBarView DataContext="{Binding Path=SearchBarViewModel}" Grid.ColumnSpan="2" Grid.Column="0" Grid.Row="0" />
        <local:PhotoFeedView DataContext="{Binding Path=FeedViewModel}" Grid.ColumnSpan="2" Grid.Column="0" Grid.Row="1"/>
            
    </Grid>
</UserControl>
