<UserControl x:Class="PhotoScope.DesktopUI.Views.SearchBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoScope.DesktopUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0" Margin="10" MinWidth="200" Text="{Binding Path=SearchWord}" HorizontalAlignment="Stretch"/>
        <Button Grid.Column="1" MinWidth="100" Command="{Binding SearchCommand}" Content="Search" Margin="10"/>
            
    </Grid>
</UserControl>
